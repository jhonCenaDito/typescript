let a: number;
let b: boolean;
let c: string;
let d: number[]; // array of numbers
let e: any;
let f: any[]; // array of any type

d = [1, 2, 3];
f = [1, true, "string"];

enum Color { Red, Green, Blue }; // Red = 0, Green = 1, and so on

let backgroundColor = Color.Blue;

console.log(backgroundColor);